-- =============================================================
-- clean.sql  –  JBS ハンズオン 後片付け
--
-- ハンズオン終了後に実行してください。
-- setup.sql および Part1〜4 で作成した全オブジェクトを削除します。
--
-- 【注意】
--   Part4（Marketplace）でデータを取得した場合、
--   末尾のコメント部分を適宜修正して実行してください。
-- =============================================================

USE ROLE ACCOUNTADMIN;

-- -----------------------------------------------
-- Part1 の学習用 WH（通常は Part1 の最後に自動削除されますが、念のため）
-- -----------------------------------------------
DROP WAREHOUSE IF EXISTS my_wh;

-- -----------------------------------------------
-- ハンズオン用 WH
-- -----------------------------------------------
DROP WAREHOUSE IF EXISTS AI_HANDSON_WH;

-- -----------------------------------------------
-- ハンズオン用 DB（内部のテーブル・ビュー・ステージ・ファイルフォーマット・
-- セマンティックビュー・Cortex エージェントを含め全て削除されます）
-- -----------------------------------------------
DROP DATABASE IF EXISTS AI_HANDSON_DB;

-- -----------------------------------------------
-- Part4（Marketplace）で取得したデータベース（オプション）
-- ※ Part4 を実施した場合のみ、コメントを外して実行してください
-- -----------------------------------------------
-- DROP DATABASE IF EXISTS <marketplace_db_name>;

-- -----------------------------------------------
-- 確認
-- -----------------------------------------------
SHOW WAREHOUSES LIKE 'AI_HANDSON_WH';
SHOW DATABASES LIKE 'AI_HANDSON_DB';
